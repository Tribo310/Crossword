"<-------------Ажлуулах команд------------------->"
1. 'pip install tk' хийж татах
    Татсан эсэх ээ шалгаж болно: Доор байгаа кодыг ажлуулж болно
    "import tkinter
        tkinter._test()"

2."Run command: python app.py "


"<-------------Git hub------------------->"
git init
git add README.md
git commit -m "first commit"
git branch -M main
git remote add origin https://github.com/Tribo310/desktop-tutorial.git
git push -u origin main

"<-------------Git hub Push------------------->"

…or push an existing repository from the command line
git remote add origin https://github.com/Tribo310/desktop-tutorial.git
git branch -M main
git push -u origin main